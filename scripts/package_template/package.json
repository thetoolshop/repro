{
  "name": "@repro/%NAME%",
  "version": "1.0.0",
  "main": "src/index.ts",
  "scripts": {
    "test": "tsx --experimental-test-module-mocks --import=global-jsdom/register --test src/**/*.test.ts{,x}",
    "fmt": "prettier --write src",
    "typecheck": "tsc --noEmit"
  },
  "license": "UNLICENSED",
  "dependencies": {},
  "devDependencies": {
    "@types/expect": "overridden",
    "@types/node": "overridden",
    "@types/repro-shared-types": "workspace:*",
    "expect": "overridden",
    "global-jsdom": "overridden",
    "prettier": "^3.0.0",
    "prettier-plugin-organize-imports": "^3.2.3",
    "ts-eager": "^2.0.2",
    "tsx": "^4.19.3",
    "typescript": "overridden"
  }
}
