{
  "name": "@repro/api-server",
  "targets": {
    "start": {
      "executor": "nx:run-script",
      "options": {
        "script": "start"
      },
      "dependsOn": ["^build"]
    },

    "watch": {
      "executor": "nx:run-script",
      "options": {
        "script": "watch"
      },
      "dependsOn": ["^build"]
    }
  }
}
