{
  "host": "${repro-prod-db.HOSTNAME}",
  "port": "${repro-prod-db.PORT}",
  "user": "${repro-prod-db.USERNAME}",
  "password": "${repro-prod-db.PASSWORD}",
  "database": "${repro-prod-db.DATABASE}",
  "ssl": {
    "rejectUnauthorized": false,
    "ca": "${repro-prod-db.CA_CERT}"
  }
}
