{
  "name": "@repro/public-playback-ui",
  "targets": {
    "build": {
      "executor": "nx:run-script",
      "options": {
        "script": "build"
      },
      "outputs": ["{projectRoot}/dist"],
      "dependsOn": ["^build"],
      "cache": true
    },
    "serve": {
      "executor": "nx:run-script",
      "options": {
        "script": "serve"
      },
      "dependsOn": ["^build"]
    }
  },
  "tags": ["type:app", "build:static"],
  "services": [
    "public-playback-ui"
  ]
}
