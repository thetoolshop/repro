apiVersion: apps/v1
kind: Deployment
metadata:
  name: {{ .Release.Name }}-deployment
  labels:
    app: {{ .Release.Name }}
spec:
  replicas: 1
  selector:
    matchLabels:
      app: {{ .Release.Name }}
  template:
    metadata:
      labels:
        app: {{ .Release.Name }}
    spec:
      containers:
        - name: {{ .Values.container.name }}
          image: {{ .Values.container.image }}
          imagePullPolicy: Never
          ports:
            - containerPort: {{ .Values.vars.PORT }}
              name: http
          env:
            - name: NODE_ENV
              value: "{{ .Values.vars.NODE_ENV }}"
            - name: HOST
              value: "{{ .Values.vars.HOST }}"
            - name: PORT
              value: "{{ .Values.vars.PORT }}"
            - name: DB_HOST
              value: "{{ .Values.vars.DB_HOST }}"
            - name: DB_PORT
              value: "{{ .Values.vars.DB_PORT }}"
            - name: DB_USER
              value: "{{ .Values.vars.DB_USER }}"
            - name: DB_PASSWORD
              value: "{{ .Values.vars.DB_PASSWORD }}"
            - name: DB_NAME
              value: "{{ .Values.vars.DB_NAME }}"
            - name: STORAGE_ENDPOINT
              value: "{{ .Values.vars.STORAGE_ENDPOINT }}"
            - name: STORAGE_REGION
              value: "{{ .Values.vars.STORAGE_REGION }}"
            - name: STORAGE_BUCKET
              value: "{{ .Values.vars.STORAGE_BUCKET }}"
            - name: STORAGE_ACCESS_KEY_ID
              value: "{{ .Values.vars.STORAGE_ACCESS_KEY_ID }}"
            - name: STORAGE_SECRET_ACCESS_KEY
              value: "{{ .Values.vars.STORAGE_SECRET_ACCESS_KEY }}"
            - name: DEBUG
              value: "{{ .Values.vars.DEBUG }}"
